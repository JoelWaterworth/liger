struct Foo {
    x mut i64

    plah:(Foo, i64) -> IO
    plah x y = {
        print(5);
    }
}

struct Bar {
    x Foo
}

main:() -> IO
main = {
    let mut y = Foo{x: 1};
    let mut x = y;
    x.x = 6;
    print(y.x);
}