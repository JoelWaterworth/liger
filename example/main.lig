struct Foo {
    x mut i64

    plah:(Foo, i64) -> IO
    plah x y = {
        print(5);
    }
}

struct Bar {
    x Foo
}

main:() -> IO
main = {
    let mut y = Bar{x: Foo{x: 1}};
    y.x.plah(5);
}