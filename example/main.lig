struct Foo {
    x: i64
    y: i64

    add::(Foo) -> i64
    add foo = {
        return foo.y
    }
}

bar::(Foo) -> i64
bar foo = {
    return foo.x
}

main::() -> IO
main = {
    let f = Foo {x: 1, y: 2}
    print(f.add())
}

